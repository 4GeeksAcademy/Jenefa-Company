check code redundancy, use console.log statements to run and check the results on terminal just like in html file for web browser.
see if every dropdown box, buttons AND THEIR ONCLICK() function runs and executes correctly.
import sample data found in sampleData.ts to test working of the buttons 
donot create new files, try to implement these logic in already created files in src folder