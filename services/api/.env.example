# Cryptographic signing secret for JWT (required). Generate a long random string.
SECRET_KEY=change-me-to-a-long-random-secret

# Access token lifespan in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Optional: override TinyDB path for User/Profile collections
# AUTH_DB_PATH=./data/auth.json
