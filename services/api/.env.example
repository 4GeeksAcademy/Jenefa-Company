SECRET_KEY=your-secret-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30
PASSWORD_RESET_EXPIRE_MINUTES=60
FRONTEND_BASE_URL=http://localhost:3001
# AUTH_DB_PATH=data/auth.json

# Clinic supply ledger (SQLModel). Use a Supabase Postgres URL in hosted environments.
# INVENTORY_DATABASE_URL=postgresql://postgres:PASSWORD@db.PROJECT.supabase.co:5432/postgres
# Local fallback is sqlite: data/inventory.db
# INVENTORY_SEED_PASSWORD=ClinicSeedPass1

# Transactional email (pick one provider — never commit real keys)
# EMAIL_PROVIDER=resend
# RESEND_API_KEY=
# EMAIL_PROVIDER=sendgrid
# SENDGRID_API_KEY=
# EMAIL_FROM=HealthCore <onboarding@resend.dev>

SECRET_KEY="dev-only-secret"
TINYDB_AUTH_DB_PATH="auth_db.json"
DB_URL=postgresql_link
